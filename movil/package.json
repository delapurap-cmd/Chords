{
  "name": "mtm-acordes",
  "version": "1.0.0",
  "description": "App Android del reconocedor de acordes de More Than Modes",
  "private": true,
  "scripts": {
    "www": "node build-www.mjs",
    "android": "npm run www && npx cap add android || npx cap sync android",
    "apk": "npm run android && cd android && ./gradlew assembleDebug"
  },
  "dependencies": {
    "@capacitor/android": "8.5.2",
    "@capacitor/core": "8.5.2"
  },
  "devDependencies": {
    "@capacitor/cli": "8.5.2"
  }
}
